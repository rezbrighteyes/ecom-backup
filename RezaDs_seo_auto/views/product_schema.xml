<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="product_schema_jsonld"
              inherit_id="website_sale.product"
              name="RezaDs SEO - Schema.org JSON-LD">
        <xpath expr="//head" position="inside">
            <t t-set="schema_availability" t-value="'https://schema.org/InStock' if product.qty_available > 0 else 'https://schema.org/OutOfStock'"/>
            <script type="application/ld+json" t-json="{
                '@context': 'https://schema.org/',
                '@type': 'Product',
                'name': product.name,
                'description': product.description_sale or product.name,
                'sku': product.default_code or '',
                'offers': {
                    '@type': 'Offer',
                    'priceCurrency': 'AUD',
                    'price': product.list_price,
                    'availability': schema_availability,
                    'seller': {
                        '@type': 'Organization',
                        'name': website.name,
                    }
                }
            }"/>
        </xpath>
    </template>
</odoo>
